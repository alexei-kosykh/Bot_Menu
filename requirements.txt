# Core Dependencies
python-telegram-bot>=20.7
gspread>=5.12.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
Pillow>=10.2.0

# Optional Dependencies (бот работает без них, но с ограниченным функционалом)

# QR Code Recognition
# Установка: pip install pyzbar
# Требует системной библиотеки zbar: apt-get install libzbar0 (Linux) или brew install zbar (macOS)
pyzbar>=0.1.9

# OCR (Optical Character Recognition)
# Установка: pip install pytesseract
# Требует Tesseract OCR: apt-get install tesseract-ocr tesseract-ocr-rus (Linux)
pytesseract>=0.3.10

# Image Processing for OCR optimization
# Установка: pip install opencv-python
opencv-python>=4.8.0

# HTTP Requests (для обработки ошибок сети)
requests>=2.31.0

# Utilities
python-dateutil>=2.8.2

# Примечание: Если optional зависимости не установлены, бот будет работать
# в ограниченном режиме с выводом предупреждений в консоль
